# Environment
.env
env/
__pycache__/
*.pyc
*.pyo

# Database (backup separately — never commit)
bus_app.db
*.db-shm
*.db-wal

# Uploaded files (store in object storage in production)
uploads/*
!uploads/.gitkeep

# Logs
logs/*
!logs/.gitkeep
